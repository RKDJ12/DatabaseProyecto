<!-- song_item.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="@color/black"
    android:padding="16dp">

    <!-- Imagen de la canción -->
    <ImageView
        android:id="@+id/songImage"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_marginEnd="16dp"
        android:contentDescription="Song image"
        android:background="@android:color/white"
        android:src="@drawable/defaultmusic" />

    <!-- Contenedor de texto y botón en columna -->
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1">

        <!-- Título de la canción -->
        <TextView
            android:id="@+id/songTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Song Title"
            android:textColor="@color/white"
            android:textSize="16sp" />

        <!-- Artista de la canción -->
        <TextView
            android:id="@+id/songArtist"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Artist"
            android:textColor="@color/white"
            android:textSize="14sp" />
    </LinearLayout>

    <!-- Botón de Play alineado a la derecha -->
    <!-- Botón de Play como ImageButton alineado a la derecha -->
    <ImageButton
        android:id="@+id/playButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/plaai"
        android:background="@android:color/transparent"
        android:contentDescription="Play" />

</LinearLayout>
