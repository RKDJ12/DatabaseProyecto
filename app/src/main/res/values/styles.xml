<resources>
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Cambiar el fondo de toda la app -->
        <item name="android:windowBackground">@android:color/black</item>
        <!-- Otros estilos de tu app -->
    </style>
</resources>
